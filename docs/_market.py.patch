# Patch generated by Pyment v0.3.3

--- a/nexm//_market.py
+++ b/nexm//_market.py
@@ -34,11 +34,12 @@
     def get(self, key: str) -> Any:
         """Get value from db by key
 
-        Args:
-            key (str): key to locate value
+        :param key: key to locate value
+        :type key: str
+        :param key: str: 
+        :returns: returns value if exist else None
+        :rtype: Any
 
-        Returns:
-            Any: returns value if exist else None
         """
 
         if not self._check_valid(key):
@@ -55,11 +56,12 @@
     def remove(self, key: str) -> bool:
         """Remove value from db by key
 
-        Args:
-            key (str): key to locate value
+        :param key: key to locate value
+        :type key: str
+        :param key: str: 
+        :returns: returns True if succes else False
+        :rtype: bool
 
-        Returns:
-            bool: returns True if succes else False
         """
 
         if not self._check_valid(key):
@@ -75,9 +77,13 @@
     def set(self, key: str, value: Any):
         """Set value by key
 
-        Args:
-            key (str): key to locate value
-            value (Any): value to save
+        :param key: key to locate value
+        :type key: str
+        :param value: value to save
+        :type value: Any
+        :param key: str: 
+        :param value: Any: 
+
         """
 
         if not self._check_valid(key):
@@ -88,14 +94,16 @@
 
     def update_interaction(self, interaction: Interaction):
         """Update interaction
-
+        
         for example:
             store = market(interaction)
             interaction.defer()
             store.update_interaction(interaction.followup)
 
-        Args:
-            interaction (Interaction): new interaction
+        :param interaction: new interaction
+        :type interaction: Interaction
+        :param interaction: Interaction: 
+
         """
 
         with shelve.open(self.db_path) as old_db:
@@ -109,8 +117,10 @@
     def _set_inter(self, interaction: Interaction):
         """Update interaction
 
-        Args:
-            interaction (Interaction): new interaction
+        :param interaction: new interaction
+        :type interaction: Interaction
+        :param interaction: Interaction: 
+
         """
 
         self.db_path = DB_PATH + str(interaction.id)
@@ -126,15 +136,14 @@
     def _check_valid(self, key: Any) -> bool:
         """Check for key is valid
 
-        Args:
-            key (Any): key for check
+        :param key: key for check
+        :type key: Any
+        :param key: Any: 
+        :returns: return True if all okay
+        :rtype: bool
+        :raises TypeError: key is not a string
+        :raises ValueError: key is blank string
 
-        Raises:
-            TypeError: key is not a string
-            ValueError: key is blank string
-
-        Returns:
-            bool: return True if all okay
         """
 
         if not isinstance(key, str):
